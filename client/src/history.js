// history package installed automatically with react-router-dom
import { createBrowserHistory } from 'history';
// create a history object that we can maintain and get access to anywhere inside our project
export default createBrowserHistory();